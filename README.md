# Valetiny 💘 (tiny Valentine website)

## Run locally
Just open `index.html` in a browser — it’s a static site.

## Customize
- Change colors + layout in `styles.css`
- Update love-note lines + playlist links in `script.js`
- Change default names/message in `index.html`

## Deploy (easy)
- GitHub Pages: push these files to a repo and enable Pages
- Netlify/Vercel: drag-and-drop the folder / import repo
